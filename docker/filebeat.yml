filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /logs/*.log   # <-- updated path

    # Handle Spring Boot stack traces (multi-line)
    multiline.pattern: '^\d{4}-\d{2}-\d{2}'
    multiline.negate: true
    multiline.match: after

output.logstash:
  hosts: ["logstash:5044"]
